/*import movies from '../api/movies.json';

export async function obtenerPeliculasPopulares(){
    return movies.peliculasPopulares.map((item)=>{
        const{poster,titulo}=item;
        return {poster, titulo}
    })
}

export async function obtenerPeliculasProximas(){
    return movies.proximamente.map((item)=>{
        const{poster,titulo}=item;
        return {poster, titulo}
    })
}

export async function obtenerPeliculasTerror(){
    return movies.terror.map((item)=>{
        const{poster,titulo}=item;
        return {poster, titulo}
    })
}

export async function obtenerPeliculasAccion(){
    return movies.accion.map((item)=>{
        const{poster,titulo}=item;
        return {poster, titulo}
    })
}

export async function obtenerPeliculasAnimacion(){
    return movies.animacion.map((item)=>{
        const{poster,titulo}=item;
        return {poster, titulo}
    })
}

export async function obtenerPeliculasRomance(){
    return movies.romance.map((item)=>{
        const{poster,titulo}=item;
        return {poster, titulo}
    })
}

export async function obtenerPeliculasRecomendados(){
    return movies.recomendados.map((item)=>{
        const{poster,titulo}=item;
        return {poster, titulo}
    })
}*/